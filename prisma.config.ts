import "dotenv/config"; // This line loads your .env file
import { defineConfig } from "prisma/config";

export default defineConfig({
  // Your Prisma configuration (if any) goes here
  // For now, it can just be empty
});